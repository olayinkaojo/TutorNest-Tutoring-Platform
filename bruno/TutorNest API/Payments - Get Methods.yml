info:
  name: Payments - Get Methods
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/make-server-cbd74580/payments/methods?Authorization=Bearer {{token}}&apikey={{anonKey}}"
  params:
    - name: Authorization
      value: Bearer {{token}}
      type: query
    - name: apikey
      value: "{{anonKey}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
