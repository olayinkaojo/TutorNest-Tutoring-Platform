info:
  name: Payouts - Dashboard
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/make-server-cbd74580/payouts/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
