info:
  name: Test Email
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/make-server-cbd74580/test-email"
  body:
    type: json
    data: |-
      {
        "email": "olayinkaojo.ng@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
